{
  "name": "workerd-slim-test",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cp node_modules/test-wasm-lib/dist/test-wasm-lib.wasm . && wrangler deploy --dry-run --outdir dist",
    "test": "true"
  }
}
